Run the full check suite for this project in sequence and report any failures:

1. `pnpm check` — svelte-kit sync + svelte-check type checking
2. `pnpm lint` — ESLint + Prettier
3. `pnpm test` — Vitest unit/component tests

Stop and report clearly if any step fails. If all pass, confirm everything is green.
